[Unit]
Description=Gunicorn instance to serve my Flask application
After=network.target

[Service]
User=nobody
Group=nogroup
WorkingDirectory={{ flask_app_dest }}
Environment="PATH=/usr/bin"
ExecStart=/usr/local/bin/gunicorn -w 4 -b 0.0.0.0:8000 myapp:app

[Install]
WantedBy=multi-user.target